export const name = 'actiontaint';
export const version = '0.1.0';
export const status = 'in-progress';

export function describe() {
  return {
    name,
    version,
    status,
    purpose: 'Local GitHub Actions workflow scanner for agentic injection risk.',
  };
}
